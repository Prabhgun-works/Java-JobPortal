# 1.Clone the repo 
    git clone https://github.com/Prabhgun-works/Java-JobPortal.git
# Run this in CLI.
 **brew install openjdk@21 maven mysql** 
# Verify Downloads 
git --version
jmeter --version
mysql --version